<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected item color -->
    <item android:color="@color/selected_color" android:state_checked="true"/>
    <!-- Default (unselected) item color -->
    <item android:color="@color/default_color"/>
</selector>
